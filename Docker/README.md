# Docker Configuration

This folder contains the Docker configuration used to containerize the React Vite application.

Files:
- Dockerfile
- .dockerignore

The Docker image is built by Jenkins during the CI/CD pipeline and pushed to Amazon ECR before deployment to Amazon EKS.
